# CLAUDE.md — 이 저장소에서 일하는 규칙

## 작업 승인 흐름 (필수)

**로컬 확인 → 보고 → 승인 → 커밋·푸시.** 이 순서를 건너뛰지 않는다.

### 1. 로컬에서 띄우고 확인한다

코드를 고쳤으면 말로 설명하지 말고 **실제로 돌려서** 동작을 확인한다. 변경 성격에 맞는 것을 고른다.

```bash
.venv/Scripts/python.exe -m pytest -q                 # 항상
.venv/Scripts/python.exe build.py <영향받는 타깃>       # 파이프라인·생성기 변경
.venv/Scripts/python.exe -m scripts.health_probe --no-gh   # SLO·센서 변경
```

- **발행물(HTML/CSS)을 바꿨으면** 브라우저로 실제 렌더를 본다. "빌드가 통과했다"는 렌더 확인이 아니다.
- **워크플로·배포 경로를 바꿨으면** 재현 환경을 만들어 검증한다(예: bare 저장소로 push 경합 재현).
  CI에서 처음 돌려보는 것은 검증이 아니다.
- **데스크톱 전용 경로**(Kiwoom OCX·자산 수집)는 32bit Windows가 필요하다. 여기서 확인 불가능한
  것을 "확인했다"고 말하지 않는다 — 무엇을 검증했고 무엇을 못 했는지 나눠서 말한다.

### 2. 증거와 함께 보고한다

통과한 테스트 수, 프로브 출력, 렌더 결과, 재현 로그 같은 **실측**을 보여준다.
실패했거나 확인하지 못한 항목이 있으면 그것도 그대로 말한다. 숨기고 넘어가지 않는다.

### 3. 승인을 기다린다

**사용자가 명시적으로 승인하기 전에는 커밋·푸시·머지하지 않는다.** "고쳐줘"는 수정 지시이지
커밋 승인이 아니다. 승인은 매번 받는다 — 한 번의 승인이 다음 작업까지 이어지지 않는다.

### 4. 커밋 직전, 커밋될 트리를 검증한다

로컬 워킹트리 테스트만으로는 부족하다. **커밋된 트리**를 꺼내서 확인한다.

```bash
rm -rf /tmp/cand && mkdir -p /tmp/cand
git archive $(git write-tree) | tar -x -C /tmp/cand
cd /tmp/cand && python -c "import build, generators.pipelines" && python -m pytest -q
```

이 단계가 필요한 이유는 실제 사고 때문이다(2026-07-29). 커밋에 신규 모듈 3개가 빠졌는데
워킹트리에는 파일이 있어 **로컬 pytest 347건이 전량 통과**했고, CI만 `ImportError`로 9시간
전면 실패했다. 새 모듈을 추가하는 커밋에서 특히 위험하다 — 참조하는 쪽만 커밋되기 쉽다.
`git status`에 `??`가 남아 있는데 그것을 import하는 파일을 커밋한다면 멈추고 함께 스테이징한다.

### 5. 남의 작업과 섞지 않는다

인덱스에 사용자가 스테이징해 둔 작업이 있을 수 있다. **`git add -A`·`git commit -a`를 쓰지 않는다.**
파일을 하나씩 명시해 스테이징하고, 커밋 후 원래 스테이징 상태를 복원한다.
한 파일 안에 내 변경과 사용자 변경이 섞였으면 훅 단위로 분리한다.

---

## 이 저장소의 사실 (자주 틀리는 것들)

- **저장소 루트는 `ai_trading_assistant/`** 다. `Th_bot/`은 상위 폴더일 뿐이고 git 저장소가 아니다.
- **`TH_DATA`는 별도 저장소**(Obsidian vault, private). 형제 폴더로 존재하며 CI는 듀얼 checkout한다.
- **main 직접 커밋**이 이 저장소의 운용 방식이다. 봇이 30~60분마다 커밋하므로 장수 브랜치는 만들지 않는다.
- **`docs/`는 전부 생성물**이다. 충돌하면 재생성하면 되므로 원격판을 받아들여도 된다.
  단 **`docs/ai-office/runlog.json`은 예외** — CI와 데스크톱이 공유하는 누적 원장이라
  덮어쓰면 상대 기록이 사라진다. `scripts/merge_remote_runlog`로 병합한다.
- **자산 절대금액은 절대 커밋하지 않는다**(design/20 Phase 8). 발행물은 암호문 단독.
- **설계가 정본이다** — `design/20`~`design/27`. 코드와 문서가 다르면 그게 이슈다.

## 루프 시스템 (design/26)

- 런타임 기준은 `config/slo.py`, 코드 품질 기준은 `config/audit_rubric.py`.
  **크론을 바꾸면 SLO의 `max_age_*`도 함께 바꾼다** — 안 그러면 정책이 곧 오탐이 된다.
- 발견한 문제는 `utils/ledger.record()`로 원장에 남긴다. 문제점·원인·해결방법·성능향상·부작용
  5필드가 강제된다 — 해결방법 없는 지적은 기록할 수 없다.
- `tier="desktop"` 이슈는 클라우드에서 검증이 불가능하므로 **고치려 들지 않는다**. 관측·보고만 한다.
