# 00. 디자인 시스템 (Global Foundation)

**AI Trading Assistant — Global Design Foundation v1.0**
적용 범위: 전체 10개 페이지(Dashboard / Macroeconomics / News / Stock / Financial Statements / Technical Analysis / Asset / Portfolio / Settings 및 하위 화면)의 단일 기준 문서.
기준 해상도: Desktop First, 1920×1080. 다크 테마 단일 운영(라이트 테마 없음).

---

## 0-1. 제품 성격 정의

Bloomberg Terminal의 정보 밀도, TradingView의 데이터 시각화, Apple의 위계와 명료함, Linear의 절제와 정밀함을 결합한 **개인 투자자용 프리미엄 트레이딩 워크스페이스**.

- 사용자는 하루 수 회, 짧게는 30초(장전 브리핑 확인), 길게는 30분(종목 분석) 단위로 접속한다.
- 따라서 모든 화면은 "3초 안에 핵심 파악 → 필요 시 파고들기(Drill-down)"의 2단 구조를 갖는다.
- 화면은 정보가 많아 보여야 하지만, 절대 복잡해 보여서는 안 된다. 이 모순의 해법은 **색의 절제 + 타이포 위계 + 균일한 그리드**다.

---

## 1. 디자인 원칙 (Design Principles)

### P1. 숫자가 주인공이다 (Numbers First)
모든 화면에서 가장 큰 시각 요소는 장식이나 일러스트가 아니라 **숫자**여야 한다.
- 실제 작동 예: Dashboard의 KOSPI 카드에서 `2,674.31`은 32px Bold, 라벨 "KOSPI"는 12px Gray다. 라벨이 숫자보다 크거나 진한 조합은 금지.
- 실제 작동 예: 카드 배경에 그라데이션·일러스트를 넣자는 요구가 오면 이 원칙으로 기각한다.

### P2. 색은 의미다 (Color = Meaning)
색은 오직 4가지 목적에만 쓴다 — ① 등락 방향 ② 상태(경고/오류/신선도) ③ 핵심 CTA ④ 차트 시리즈 구분. 장식 목적의 색 사용은 전면 금지.
- 실제 작동 예: 뉴스 카드의 카테고리 태그는 색이 아니라 회색 아웃라인 배지로 구분한다. 색을 쓰는 순간 "등락인가?"라는 오독이 생기기 때문이다.
- 규칙 문장(§2-6)으로 강제한다.

### P3. 신선도는 데이터의 일부다 (Freshness is Data)
값과 시각(as-of)은 한 몸이다. 언제 데이터인지 모르는 숫자는 틀린 숫자보다 위험하다.
- 실제 작동 예: 야간선물 값이 시장 마감 후에도 흰색 큰 숫자로 표시되어 "지금 값"처럼 오독된 실제 사고가 있었다. 이후 모든 시세 컴포넌트는 세션 인식(Session-aware) 렌더링을 의무화한다(§9-4).

### P4. 위계는 밝기로, 구분은 보더로 (Hierarchy by Luminance)
다크 테마에서 그림자는 거의 보이지 않는다. 깊이는 **표면 밝기 단계 + 1px 보더**로 표현하고, 그림자는 오버레이 레이어(드롭다운·모달)에만 허용한다(§4-3).
- 실제 작동 예: 카드 위에 뜨는 팝오버는 카드보다 한 단계 밝은 표면(#2A3648)과 그림자를 함께 갖는다. 같은 평면의 카드끼리는 그림자를 절대 쓰지 않는다.

### P5. 3클릭 도달, 1화면 요약 (3-Click Reach)
어떤 정보든 3클릭 이내 도달해야 하며, 각 페이지 첫 화면(스크롤 0 상태)은 그 페이지의 결론 요약을 담는다.
- 실제 작동 예: "삼성전자 부채비율" 도달 경로 = 헤더 검색에서 종목 선택(1클릭, Enter) → 선택 즉시 Stock Hub 패널 오픈(추가 클릭 없는 인터스티셜) → 패널 내 "재무제표 분석" 버튼(2클릭). 3클릭 예산 중 2클릭만으로 도달하는 여유 사례이며, 이보다 깊어지는 IA는 재설계 대상. (※ "Financial Statements 탭"과 같이 실재하지 않는 컴포넌트를 예시에 인용하지 않는다 — 05 Stock Hub §1-1 기준 실제 IA를 따른다.)
- 실제 작동 예: Macroeconomics 첫 화면 상단엔 항상 "오늘의 매크로 한 줄 요약" Hero 카드가 온다.

---

## 2. 컬러 시스템 (Color System)

### 2-1. 표면(Surface) 토큰

매트 차콜 계단이다. 완전한 블랙을 쓰지 않는 이유는 두 가지다 — 장시간 응시할 때 눈이 덜 피로하고, 카드가 배경에서 살짝 떠 보여 "정보가 시스템적으로 묶여 있다"는 인상이 생긴다.

| 토큰명 | HEX | 용도 | 사용 금지 사례 |
|---|---|---|---|
| `surface.base` | #141519 | 앱 전체 배경, 사이드바·헤더 배경 | 카드 내부 배경으로 사용 금지 |
| `surface.card` | #1E2026 | 모든 카드·패널 기본 배경 | 페이지 전체 배경으로 사용 금지 |
| `surface.card-hover` | #262931 | 카드·행 호버 배경 | 정적(비인터랙티브) 요소 배경 금지 |
| `surface.elevated` | #2E313A | 드롭다운, 팝오버, 모달, 툴팁 | 일반 카드 배경으로 남용 금지 |
| `surface.sunken` | #0F1013 | 입력 필드 내부, 표 내부 웰, 코드 영역 | 텍스트 대비 확보 없이 본문 배경 금지 |
| `surface.selected` | #DFB76C 10% 오버레이 | 선택된 행·탭·리스트 아이템 | 호버와 동시 의미로 혼용 금지 |
| `surface.overlay-panel` | #23262E | 대면적 비모달 오버레이(Stock Hub) 전용 | 일반 카드 배경 금지 |

### 2-2. 텍스트 토큰

| 토큰명 | HEX | card 위 대비 | 용도 | 사용 금지 사례 |
|---|---|---|---|---|
| `text.primary` | #F8F9FA | 15.45:1 | 핵심 숫자, 제목, 본문 강조 | 보조 설명·캡션에 남용 금지(위계 붕괴) |
| `text.secondary` | #A0A5B1 | 6.60:1 | 라벨, 캡션, 보조 설명, stale 처리된 값 | 핵심 시세 숫자에 사용 금지(단, stale 상태 예외) |
| `text.tertiary` | #858C9B | 4.82:1 | 타임스탬프, 플레이스홀더, 비활성 힌트 | 12px 미만 크기와 조합 금지 |
| `text.disabled` | #4A4F5C | 1.99:1 | 비활성 버튼·메뉴 텍스트 | 읽혀야 하는 정보에 사용 금지 |
| `text.inverse` | #141519 | — | 골드 채움 버튼 위 텍스트(골드 위 9.67:1) | 어두운 배경 위 사용 금지 |

### 2-3. 브랜드·액센트 토큰

브랜드는 **딥 골드**다. 노란색에 가까운 금색이 아니라 채도를 낮춘 매트 골드이며, 이 색이 제품의 "차분하고 똑똑한" 성격을 혼자 짊어진다.

| 토큰명 | HEX | 용도 | 사용 금지 사례 |
|---|---|---|---|
| `brand.primary` | #DFB76C | 핵심 CTA 버튼, 활성 메뉴 인디케이터, **화면 최상위 수치 1개** | **등락 표시에 사용 금지(역할 분리)** |
| `brand.primary-hover` | #ECCB8A | CTA 호버, 링크 호버, 포커스 링 | 넓은 면적 배경 금지 |
| `brand.primary-pressed` | #C39A52 | CTA 눌림 상태 | — |
| `accent.yellow` | #E8703A | 주의·지연 배지, 하이라이트 | 본문 텍스트 색 금지, CTA 금지 |
| `accent.violet` | #A78BFA | 분석 인사이트·요약 등 "해석 레이어" 전용 강조 | 시세·등락 관련 요소 금지 |

`accent.yellow`는 이름과 달리 **오렌지**다. 브랜드가 골드가 된 이상 노란 경고색은 브랜드 포인트와 구분되지 않기 때문이다(골드 ↔ 오렌지 OKLab ΔE 16.0 실측). 토큰명을 유지한 것은 참조 지점을 놓치지 않기 위한 실용적 판단이다.

#### 2-3-1. `accent.violet` "해석 레이어" 사용 원칙 (특별함 유지 규칙)

`accent.violet`은 "시스템이 데이터를 대신 해석해 짚어준 판단"이라는 단 하나의 신호를 위해 존재하는 색이다. 좌측 3px 바 + overline + 문장으로 구성된 동일한 박스가 페이지마다 반복 등장하면, 그 자체가 하나의 정형화된 위젯으로 소비되어 신호력이 사라진다.

- **전용 페이지 우선 배정**: 좌측 바를 갖춘 "정식" 해석 레이어 박스는 **Dashboard(모닝 요약)와 Financial Statements(진단 요약)** 두 곳을 1차 용도로 삼는다.
- **그 외 페이지는 기본을 일반 캡션으로**: 나머지 페이지에서 유사한 시스템 코멘트가 필요하면 기본값은 좌측 바 없는 **일반 캡션**(`type.caption` + `text.secondary`)이다.
- **불가피하게 확장할 경우 형태를 변주한다**: 좌측 3px 바 + 회색 텍스트의 완전히 동일한 조합을 5개 이상 페이지에 반복 적용하는 것을 금지한다.

### 2-4. 상태(Semantic) 토큰

| 토큰명 | HEX | 용도 | 사용 금지 사례 |
|---|---|---|---|
| `status.success` | #34D399 | 저장 완료, 동기화 성공, 라이브 점 | 등락 상승 표시 금지(한국 모드 기준) |
| `status.warning` | #E8703A | 지연 데이터, 주의 알림 | — |
| `status.danger` | #F87171 | 오류, 삭제, 손실 경고 텍스트 | 등락 하락 표시 금지(한국 모드 기준) |
| `status.info` | #63C6E8 | 안내 배너, 정보 툴팁 | — |

### 2-5. 등락(Market) 토큰 — ★기본값 결정

**결정: 기본값은 한국 관습 — 상승 = 빨강, 하락 = 파랑.** Settings에서 "글로벌 모드(상승=초록/하락=빨강)"로 전환 가능.

근거:
1. **주 사용자의 지배적 멘탈 모델**: 본 제품의 1차 사용자는 국내 HTS/MTS(키움 등)로 훈련된 한국 개인 투자자다. 등락 색 오독은 이 제품에서 발생 가능한 가장 비싼 실수이며, 기본값은 사용자의 기존 반사신경을 따라야 한다.
2. **브랜드 색과의 충돌 회피**: Primary Green(#10B981)은 CTA·브랜드 색이다. 글로벌 모드를 기본으로 하면 "초록 = 상승"과 "초록 = 버튼"이 한 화면에서 충돌한다. 한국 모드에서는 등락(빨강/파랑)과 브랜드(초록)가 완전히 분리되어 색 체계가 더 깨끗하다.
3. **한·미 통합 화면의 일관성**: 미국 종목도 한국 관습 색으로 통일 표기한다. 시장별로 색 관습을 섞는 것이 최악의 선택이다.

| 토큰명 | 한국 모드(기본) | 글로벌 모드 | 용도 |
|---|---|---|---|
| `market.up` | #EF4444 | #10B981 | 상승 텍스트·아이콘 (▲와 항상 병행) |
| `market.up-bg` | #EF4444 14% 오버레이 | #10B981 14% | 상승 셀·배지 배경 |
| `market.down` | #6BB6FF | #EF4444 | 하락 텍스트·아이콘 (▼와 항상 병행) |
| `market.down-bg` | #6BB6FF 14% 오버레이 | #EF4444 14% | 하락 셀·배지 배경 |
| `market.flat` | #A0A5B1 | #A0A5B1 | 보합 (0.00%) |

> **브랜드가 골드로 바뀌며 위 근거 2가 소멸했다.** "Primary Green과 초록=상승이 충돌한다"는 논거는 브랜드가 딥 골드가 된 시점에 성립하지 않는다. 그럼에도 **기본값은 한국 모드를 유지한다** — 근거 1(국내 HTS로 훈련된 사용자의 반사신경, 등락 오독이 이 제품에서 가장 비싼 실수)과 근거 3(한·미 통합 화면의 일관성)은 브랜드 색과 무관하게 유효하기 때문이다. 글로벌 모드가 에메랄드(#10B981)/레드(#EF4444) 조합이다.
>
> `market.up`(#EF4444)의 card 위 대비는 4.33:1로 본문 기준 4.5:1에 근소하게 미달한다. R4가 ▲▼ 병행을 강제해 방향 정보가 색 단독에 의존하지 않는 것이 완화 조건이다.

- 등락 색은 텍스트·아이콘·미니 배경에만 쓴다. 카드 전체 배경을 등락 색으로 칠하는 것 금지.
- 글로벌 모드 전환 시 차트 캔들 색도 동일 규칙으로 일괄 전환된다.

### 2-6. 색 절제 원칙 (규칙 문장)

> **R1. 한 화면(뷰포트 기준)에 동시에 존재하는 '의미 색'은 4종 이내로 제한한다.** (등락 2색 + 상태 1색 + CTA 1색이 상한)
> **R2. 장식 목적의 유채색 사용을 금지한다.** 구분이 필요하면 회색 단계·보더·타이포 굵기를 먼저 쓴다.
> **R3. 핵심 CTA(Primary Green 채움 버튼)는 한 화면에 1개만 존재한다.** 나머지 액션은 아웃라인/고스트 버튼.
> **R4. 등락 색은 반드시 ▲▼ 기호 또는 +/− 부호와 병행한다.** 색 단독으로 방향을 전달하지 않는다.
> **R5. 차트 시리즈 색은 §2-7에 정의된 전용 5색 팔레트(`chart.series-1~5`)만 사용한다.** 등락(`market.*`)·상태(`status.*`)·브랜드·액센트(`brand.*`/`accent.*`) 토큰과 헥스값이 절대 겹치지 않는 별도 계열이며, 6번째 시리즈부터는 팔레트의 회색 항목(`chart.series-5`) 재사용 + 라벨 직접 표기.
> **R6. 색 토큰을 신설·변경할 때는 반드시 §2-1~§2-8의 기존 전체 헥스값과 대조해 중복 여부를 검증한다(빌드 타임 린트 대상).** 하나의 헥스값이 등락·상태·브랜드·차트라는 서로 다른 의미 카테고리에 동시에 배정되는 것은 그 자체로 결함이다 — 색은 의미다(P2)라는 원칙이 색상표 레벨에서 깨지는 것을 사전 차단한다.

### 2-7. 차트 시리즈 토큰 (Chart Series Palette)

차트 시리즈 색은 "무엇이 좋고 나쁜지"를 말하지 않는 순수 구분용 색이어야 한다. 등락·상태·브랜드·액센트 의미 토큰과 헥스값이 하나도 겹치지 않는 전용 5색 팔레트를 별도로 정의한다(R5/R6 근거 — 구 팔레트는 `market.down`(한국 모드 #60A5FA)·`status.info`(#60A5FA)·`status.success`/`brand.primary-hover`(#34D399)·`badge.live`(#34D399)와 헥스가 겹쳐 "하락"과 "차트 시리즈"가 동일 색으로 오독될 수 있었다).

| 토큰명 | HEX | 색상 계열 | 용도 |
|---|---|---|---|
| `chart.series-1` | #32A0C5 | 스틸 블루 | 1번째 시리즈(기본) |
| `chart.series-2` | #8A9C4A | 올리브 | 2번째 시리즈 |
| `chart.series-3` | #788ED6 | 페리윙클 | 3번째 시리즈 |
| `chart.series-4` | #C38148 | 브론즈 | 4번째 시리즈 |
| `chart.series-5` | #AC7DC0 | 모브 | 5번째 시리즈 |
| `chart.series-other` | #6B7280 | 중성 회색 | **시리즈 색이 아니다.** "기타 N종목"처럼 묶음을 표현할 때만 쓰고 라벨을 반드시 병기한다 |

**이 5색은 눈으로 고르지 않았다.** OKLab 기반 게이트(밝기 밴드 0.48~0.67, 채도 하한 0.10, 색각이상 분리, 정상시야 분리, 표면 대비)를 모두 통과하는 조합을 탐색해 확정했다. 실측:

| 게이트 | 결과 |
|---|---|
| 밝기 밴드 | 5색 전부 L 0.66 (밴드 내) |
| 채도 하한 | 5색 전부 ≥ 0.10 |
| 색각이상 분리(protan/deutan) | 최소 인접 ΔE **17.2** (목표 8, 하한 6) |
| 정상시야 분리 | 최소 인접 ΔE **17.3** (하한 15) |
| 표면 대비 | 5색 전부 ≥ 3:1 |

구 팔레트(#2DD4BF/#E879F9/#FB923C/#818CF8/#94A3B8)는 series-4↔series-5가 **정상시야 ΔE 13.0으로 하한 미달**이었다 — 색각이 정상인 사람도 두 색을 구분하기 어려웠다는 뜻이다. 또 구 series-5는 채도 0.035로 회색처럼 읽혀 식별 기능을 하지 못했다. 이번 개편에서 두 결함을 함께 해소했다.

시리즈 색 일부는 시맨틱 색과 계열이 겹친다(예: series-3 페리윙클 ↔ 한국 모드 하락 파랑 ΔE 10.9). 골드·에메랄드·레드가 색상환의 상당 부분을 선점한 결과이며, **형태로 분리**해 해소한다 — 시리즈 색은 항상 **면 채움**(스택 바 세그먼트, 범례 칩)으로만, 등락 색은 **텍스트 + ▲▼**로만 나타난다. 두 역할이 같은 형태로 등장하는 배치를 금지한다.

- 등락 의미가 있는 단일 계열 차트(예: 종목 하나의 가격 라인만 그릴 때)는 이 팔레트가 아니라 `market.up`/`market.down`/`text.primary`를 상황에 맞게 쓴다 — 차트 팔레트는 **복수 시리즈를 서로 구분**할 때만 사용한다.
- `chart.series-1`(청록)은 브랜드 그린(`brand.primary` #10B981, `brand.primary-hover` #34D399)과 헥스·색상 계열이 모두 다르므로 "브랜드 = 차트 기본색"으로 오독되지 않는다.
- 다크 테마 카드 배경(`surface.card` #1E2026) 위에서 5색 모두 최소 대비 3:1을 충족한다(§11-1 UI 요소 기준과 동일 잣대).
- Asset 90일 추이 차트, USD/KRW 30일 차트, EPS 5년 라인 등 페이지 전반의 다계열 차트는 모두 이 팔레트만 인용한다. 페이지 문서에서 개별 헥스값을 새로 정의하지 않는다.

### 2-8. 기타 파생 토큰

| 토큰명 | HEX / 값 | 용도 |
|---|---|---|
| `border.subtle` | #2A2D35 | 카드 내부 디바이더, 표 행 구분선, **차트 격자선** |
| `border.default` | #383C47 | 카드 외곽 보더, 입력 필드 보더 |
| `border.strong` | #4A4F5C | 호버된 입력 필드, 강조 구분 |
| `focus.ring` | #ECCB8A, 2px, offset 2px | 키보드 포커스 링(전 컴포넌트 공통) |
| `skeleton.base` | #2A2D35 | 스켈레톤 블록 기본색 |
| `skeleton.highlight` | #383C47 | 스켈레톤 시머(shimmer) 하이라이트 |
| `overlay.scrim` | #000000 66% | 모달 뒤 스크림(Level 3 오버레이 전용, §4-3) |
| `overlay.gate` | #000000 48% | 잠금 콘텐츠 게이트(Asset·Portfolio 본문) 스크림. `overlay.scrim`보다 옅게 하여 배경 스켈레톤 형태가 지각되게 함(§9-1) |
| `badge.live` | #34D399 | 라이브 세션 점(pulse), **데이터가 FRESH일 때만** 표시(§9-2 S2) |

---

## 3. 타이포그래피 (Typography)

### 3-1. 폰트 스택

- 한글: **Pretendard** / 라틴·숫자: **Inter** (숫자·영문 우선 매칭 후 한글 폴백)
- **문자와 숫자의 서체를 분리한다.** 본문·라벨은 Pretendard, 시세·수익률 같은 숫자는 Inter(폴백 Roboto)를 쓴다. 숫자가 세로로 나열될 때 열이 정확히 맞아떨어지는 것이 "정교한 프로그램"이라는 인상의 상당 부분을 만든다.
- 숫자 스타일은 기본적으로 **Tabular Figures(고정폭 숫자)** 를 쓴다 — 표·리스트·등락률에서 자릿수 정렬이 흔들리는 것을 금지하기 위한 전역 원칙.
- **예외: `type.num-display`(화면 최상위 단일 수치)는 Proportional Figures를 쓴다.** 고정폭은 모든 숫자에 0의 폭을 주므로 44px 크기에서 `121` 같은 값이 눈에 띄게 헐거워 보인다. 자릿수 정렬이 필요한 곳은 열(column)이지 hero가 아니다.
- 사용 굵기: 400 / 500 / 600 / 700 네 단계만. 300 이하·800 이상 금지.

### 3-2. 타입 스케일

| 토큰명 | 크기/굵기/행간 | 용도 | 비고 |
|---|---|---|---|
| `type.display` | 32px / 700 / 40px | Hero 카드 대표 숫자·헤드라인 | 한 화면 1~2회만 |
| `type.h1` | 24px / 700 / 32px | 페이지 타이틀 | 페이지당 1개 |
| `type.h2` | 20px / 600 / 28px | 섹션 타이틀, Hero 카드 제목 | |
| `type.h3` | 16px / 600 / 24px | 카드 타이틀 | 카드 헤더 표준 |
| `type.body` | 14px / 400 / 22px | 본문, 뉴스 요약 | 기본 텍스트 |
| `type.body-strong` | 14px / 600 / 22px | 본문 내 강조, 표 헤더 | |
| `type.caption` | 12px / 400 / 18px | 타임스탬프, 라벨, 각주 | `text.secondary` 이상과 조합 |
| `type.overline` | 11px / 600 / 14px, 자간 +0.4px | 카테고리 라벨(대문자) | 색 대신 쓰는 구분 수단 |
| `type.num-display` | 44px / 700 / 52px, proportional | 지수·대표 시세 | Hero 전용 |
| `type.num-lg` | 24px / 600 / 32px, tabular | Standard 카드 대표 값 | |
| `type.num-md` | 16px / 500 / 24px, tabular | 표 내 시세, 리스트 값 | |
| `type.num-sm` | 13px / 500 / 20px, tabular | 등락률 배지, 보조 수치 | |
| `type.num-xs` | 12px / 500 / 16px, tabular | 미니 차트 축 라벨 | 최소 크기, 이하 금지 |

### 3-3. 숫자·통화 포맷 규칙

| 항목 | 규칙 | 예시 |
|---|---|---|
| 등락 부호 | 변화값에는 부호 필수. 마이너스는 하이픈이 아닌 **진짜 마이너스(−, U+2212)** | +1.24% / −0.86% |
| 등락 기호 | 색과 함께 ▲▼ 병행, 보합은 부호 없이 회색 | ▲ 412.50 / ▼ 0.86% |
| 지수 | 소수 2자리, 천 단위 콤마 | 2,674.31 / 19,003.65 |
| 원화 시세 | ₩ 선행, 콤마, 원 단위 정수 | ₩84,300 |
| 달러 시세 | $ 선행, 소수 2자리 | $192.42 |
| 원화 대규모 금액 | 조·억 단위, 소수 1자리 | 시총 412.5조 / 거래대금 8,240억 |
| 달러 대규모 금액 | T/B/M 단위, 소수 1자리 | $3.2T / $412.5B / $88.4M |
| 환율 | 소수 2자리 + 원 | 1,378.50원 |
| 퍼센트 | 등락률 소수 2자리, 매크로 지표 소수 1자리 | −0.86% / CPI 3.1% |
| 거래량 | 한국 종목 만·억 주, 미국 종목 M 주 | 1,250만주 / 42.1M |
| 날짜·시각 | `MM/DD HH:mm` + 타임존 병기 | 07/10 08:42 KST |

- 한 컴포넌트 안에서 한국·미국 단위 표기를 섞지 않는다(종목의 상장 시장 기준을 따른다).
- 숫자 색: 절대값(현재가)은 `text.primary`, 변화값(등락)만 `market.*` 색.

---

## 4. 스페이싱 · 라운드 · 엘리베이션

### 4-1. 스페이싱 — 4pt 스케일

`4 / 8 / 12 / 16 / 20 / 24 / 32 / 40 / 48 / 64` (px) 외의 임의 값 금지.

| 용도 | 값 |
|---|---|
| 카드 내부 패딩 (Standard) | 20px |
| 카드 내부 패딩 (Hero) | 24px / (Compact) 16px |
| 카드 간 간격 (그리드 거터와 동일) | 24px |
| 라벨 ↔ 값 수직 간격 | 4px |
| 섹션 간 수직 간격 | 32px |
| 아이콘 ↔ 텍스트 간격 | 8px |
| 표 셀 좌우 패딩 | 12px |

### 4-2. 라운드(Radius)

| 토큰 | 값 | 대상 |
|---|---|---|
| `radius.card` | 14px | 카드, 패널, 모달 — 모듈형 카드 레이아웃의 기본 |
| `radius.control` | 8px | 버튼, 입력 필드, 탭 |
| `radius.badge` | 999px | 배지, 필, 상태 점 캡슐 |
| `radius.inner` | 8px | 카드 내부 웰, 툴팁, 미니 차트 컨테이너 |

### 4-3. 엘리베이션 — 다크 테마 원칙

**같은 평면에서는 그림자를 쓰지 않는다. 깊이 = 표면 밝기 + 보더.**

| 레벨 | 표면 | 보더 | 그림자 | 대상 |
|---|---|---|---|---|
| Level 0 | `surface.base` | — | 없음 | 페이지 배경 |
| Level 1 | `surface.card` | `border.default` 1px | 없음 | 카드, 패널 |
| Level 2 | `surface.elevated` | `border.strong` 1px | 0 8px 24px 검정 40% | 드롭다운, 팝오버, 툴팁 |
| Level 3 | `surface.elevated` | `border.strong` 1px | 0 16px 48px 검정 55% + 스크림 | 모달, 커맨드 팔레트 |

---

## 5. 레이아웃 그리드

### 5-1. 1920px 기준 골격

```
┌──────────┬──────────────────────────────────────────────┐
│          │  헤더 (높이 64px, 상단 고정)                    │
│ 사이드바   ├──────────────────────────────────────────────┤
│ 240px    │  콘텐츠 영역                                    │
│ 좌측 고정  │  최대폭 1600px, 좌우 마진 32px, 중앙 정렬        │
│          │  12컬럼 그리드 / 거터 24px                      │
└──────────┴──────────────────────────────────────────────┘
```

| 항목 | 값 |
|---|---|
| 사이드바 폭 | 240px (접힘 64px) |
| 헤더 높이 | 64px |
| 콘텐츠 최대폭 | 1600px |
| 그리드 | 12컬럼 / 거터 24px / 좌우 마진 32px |
| 1컬럼 실폭 (1920, 사이드바 펼침) | 약 112px |
| 페이지 상단 여백 (헤더 아래) | 24px |

### 5-2. 반응형 — 세 구간

이전 셸은 브레이크포인트가 1280px 하나뿐이었고 그 아래로는 사이드바만 64px로 줄었다. 실측 결과 세 가지가 동시에 깨졌다:

- 접힌 사이드바에 **아이콘이 없어 글자만 잘렸다** — 어느 메뉴인지 알 수 없었다.
- 390px 화면에서 `v2-span-4` 카드의 실폭이 **82px** — 숫자 한 줄도 들어가지 않았다.
- 콘텐츠 좌우 패딩이 32px 고정이라 좁은 화면일수록 본문 폭을 더 깎았다.

| 구간 | 폭 | 사이드바 | 스팬 접힘 |
|---|---|---|---|
| 데스크톱 | ≥1280px | 펼침 240px(아이콘+라벨) | 원래 스팬 유지 |
| 태블릿 | 1024~1279px | **아이콘 레일 64px**(라벨 숨김, `title`로 이름 노출) | span-3 → 4 |
| 모바일 | <1024px | **서랍**(오버레이 280px, 라벨 복원) | span-3·4 → 6 / span-6·8 → 12 |
| 소형 | <720px | 서랍 | 전부 12(1열) |

규칙:

- **아이콘은 모든 nav 항목의 필수 필드다.** 접힘 모드에서 유일하게 남는 식별 수단이므로 아이콘 없는 항목을 추가하지 않는다(`config/nav.py`의 `NavItem.icon`).
- 서랍은 **라벨을 되살린 상태**로 연다. 아이콘 레일은 호버로 이름을 확인할 수 있지만 터치에는 호버가 없다 — 좁은 화면일수록 라벨이 더 필요하다.
- 서랍이 열리면 배경 스크롤을 잠그고, Esc·스크림 클릭·메뉴 선택으로 닫힌다. 포커스는 서랍 안으로 옮기고 닫을 때 원래 자리로 돌려준다.
- 간격·좌우 패딩·`type.num-display`는 `clamp()`로 **연속 변한다**(단계적 점프 없음). 핵심 수치는 44px→30px까지 줄어든다.
- **그리드 아이템에는 항상 `min-width: 0`을 건다.** 기본값 `auto`면 카드 안의 넓은 표·긴 숫자가 트랙을 밀어 이웃 카드를 찌그러뜨리고 결국 가로 스크롤을 만든다. 스팬 유틸에 일괄 적용한다.
- **가로 스크롤은 결함 신호다.** 넓은 표·차트는 `.v2-scrollx` 안에서 자기만 스크롤하고, 페이지 본체는 절대 가로로 밀리지 않는다.
- <720px에서는 헤더 시계를 숨긴다 — 정보 밀도 대비 폭을 많이 먹고, 그 폭은 검색에 양보하는 편이 낫다.

## 6. 사이드바 상세 스펙

### 6-1. 구조 (펼침 240px)

```
┌────────────────────────┐
│ 로고 영역 (높이 64px)      │  심볼 24px + 워드마크 16px/700, 좌 패딩 20px
├────────────────────────┤
│ Dashboard              │  ← 메뉴 항목 ×6 (일반)
│ Macroeconomics         │
│ News                   │
│ Stock                  │
│ Financial Statements   │
│ Technical Analysis     │
│ ── 디바이더 (border.subtle) ──
│ Asset          [자물쇠]  │  ← 잠금 그룹 ×2
│ Portfolio      [자물쇠]  │
├────────────────────────┤
│ 하단 영역                │  Settings / 접기 토글 / 데이터 버전
└────────────────────────┘
```

### 6-2. 메뉴 항목 스펙

| 속성 | 값 |
|---|---|
| 항목 높이 | 40px, 좌우 마진 12px, 내부 패딩 좌 12px, radius 8px |
| 아이콘 | 20×20px, 텍스트와 간격 12px |
| 라벨 | 14px / 500 |
| 기본 상태 | 아이콘·라벨 `text.secondary`, 배경 투명 |
| 호버 | 배경 `surface.card-hover`, 라벨 `text.primary` (트랜지션 120ms) |
| 활성 | 배경 `brand.primary` 10% 오버레이, 좌측 인디케이터 바 3×20px `brand.primary`(radius 2px), 아이콘 `brand.primary`, 라벨 `text.primary` 600 |
| 눌림 | 배경 오버레이 14% |

### 6-3. 잠금(Locked) 항목 — Asset · Portfolio

- 기본: 라벨 `text.tertiary`, 우측 끝에 자물쇠 아이콘 14px `text.tertiary`. 아이콘·라벨 모두 톤 다운되어 "접근 가능하지만 잠겨 있음"을 표현.
- 호버: 배경 호버는 동일하게 적용하되 라벨은 `text.secondary`까지만 상승(완전 흰색 금지 — 잠김 상태 유지 암시).
- 클릭: 비밀번호 게이트 모달(Level 3) 표시. 입력 필드 1개 + 확인 버튼, 실패 시 필드 보더 `status.danger` + 흔들림 없이 헬퍼 텍스트로 안내.
- 해제 후(세션 유지): 자물쇠 아이콘이 열린 자물쇠 14px `text.tertiary`로 교체, 항목은 일반 메뉴와 동일 스타일. 세션 종료 시 재잠금.

### 6-4. 접힘(Collapsed) 모드 — 64px

- 아이콘만 중앙 정렬(20px), 라벨 숨김. 호버 시 우측에 툴팁(Level 2, 지연 300ms)으로 메뉴명 표시.
- 활성 표시는 동일한 좌측 3px 바 + 아이콘 `brand.primary`.
- 잠금 항목은 아이콘 우상단에 6px 자물쇠 도트 오버레이.
- 토글: 하단 영역의 접기 버튼(28×28px) 또는 단축키 `[`.

### 6-5. 하단 영역

- Settings 항목(일반 메뉴와 동일 스펙, 톱니 아이콘).
- 접기 토글 버튼.
- 데이터 버전 캡션: `type.caption` / `text.tertiary` — 예: "데이터 생성 07/10 07:12 KST". 클릭 시 데이터 신선도 패널(§9) 열림. 사이드바가 접히면 숨김.

---

## 7. 헤더 상세 스펙

높이 64px, 배경 `surface.base`, 하단 보더 `border.subtle` 1px, 콘텐츠와 정렬된 좌우 패딩 32px. 좌측부터:

### 7-1. 페이지 컨텍스트 (좌)

- 현재 페이지 타이틀 `type.h3` + 상위 경로가 있을 경우 브레드크럼(`text.tertiary` 12px, 구분자 "/"). 예: `Stock / 삼성전자 005930`.

### 7-2. 글로벌 검색 (중앙 좌측)

| 속성 | 값 |
|---|---|
| 필드 | 폭 360px, 높이 36px, 배경 `surface.sunken`, 보더 `border.default`, radius 8px |
| 플레이스홀더 | "종목·뉴스·페이지 검색" + 우측 끝 단축키 캡슐 `Ctrl K` (11px, `border.default` 아웃라인) |
| 포커스 | 폭 480px로 확장(200ms), `focus.ring` 적용 |
| 결과 패널 | Level 2 오버레이, 폭 560px. 그룹 3종: **종목**(티커·현재가·등락 미리보기) / **뉴스**(헤드라인·시각) / **페이지**(메뉴 바로가기). 그룹당 최대 5건, 위→아래 키보드 탐색, Enter 이동, Esc 닫기 |
| 검색 예시 | "삼성" 입력 → 종목: 삼성전자 ₩84,300 ▲0.95% · 삼성SDI / 뉴스: "삼성전자, HBM4 양산 일정 발표" 07:20 / 페이지: Financial Statements |

### 7-3. 듀얼 시계 + 시장 상태 (중앙 우측)

```
● KST 09:42 장중     ○ ET 20:42 장후
```

- 시각: `type.num-sm` tabular, `text.primary`. 라벨(KST/ET): `type.caption` `text.secondary`.
- 상태 인디케이터: 8px 원형 점 + 상태 텍스트 12px.

| 상태 | 점 색 | 한국 표기 | 미국 표기 |
|---|---|---|---|
| 장전 | `accent.yellow` | 장전 (08:00–09:00) | 프리마켓 (04:00–09:30 ET) |
| 장중 | `badge.live` (2s 느린 펄스) | 장중 (09:00–15:30) | 정규장 (09:30–16:00 ET) |
| 장후 | `accent.yellow` | 시간외 (15:40–18:00) | 애프터마켓 (16:00–20:00 ET) |
| 휴장 | `text.tertiary` | 휴장 | 휴장 |

- 펄스 애니메이션은 실제 세션 중에만 허용(§9-4의 라이브 표시 원칙과 동일 규칙).
- 클릭 시 팝오버: 다음 개장까지 카운트다운 + 금일 휴장 여부 + 야간선물 세션 상태.

### 7-4. 사용자 메뉴 (우)

- 아바타 32px 원형(이니셜 텍스트). 클릭 → Level 2 드롭다운: 등락 색상 모드(한국/글로벌) 토글, Settings 바로가기, 잠금 세션 잠그기.

---

## 8. 카드 아나토미 (Card Anatomy)

### 8-1. 공통 구조

```
┌─────────────────────────────────────────┐
│ 헤더: 타이틀(h3 16/600) ── 신선도 배지 ── 액션 │  높이 48px (패딩 포함)
│       타임스탬프 12px text.tertiary        │
├─────────────────────────────────────────┤
│ 본문: 등급별 콘텐츠                          │
├─────────────────────────────────────────┤
│ 푸터(선택): "전체 보기 →" 링크 13px          │  높이 40px, 상단 border.subtle
└─────────────────────────────────────────┘
```

- 헤더 액션: 아이콘 버튼 28×28px(히트 영역 32px), 최대 2개(새로고침·더보기). 3개 이상 필요하면 "더보기" 메뉴로 수납.
- 타임스탬프는 모든 데이터 카드에서 **생략 불가**(§9-2).
- 카드 보더 `border.default` 1px, 배경 `surface.card`, radius 12px. 카드 전체가 클릭 대상일 때만 호버 배경 적용.

### 8-2. 카드 등급 체계

| 등급 | 그리드 스팬 | 최소 높이 | 패딩 | 대표 타이포 | 용도 예시 |
|---|---|---|---|---|---|
| **Hero** | 8~12col | 320px | 24px | `num-display` 44px | 모닝리포트 요약, 지수 종합 현황 |
| **Standard** | 4~6col | 220px | 20px | `num-lg` 24px | 환율·유가 카드, 뉴스 리스트 카드, 차트 카드 |
| **Compact** | 3~4col | 120px | 16px | `num-md` 16px + 스파크라인 | 단일 지표(KOSPI, S&P 500, 달러인덱스) |
| **Row** | 부모 카드 내부 | 행 52px (밀집 40px) | 좌우 16px | `num-md` 16px | 관심종목 리스트, 뉴스 행, 매매일지 행 |

- **등급 간 타이포 위계 규칙**: Compact는 Standard보다 한 단계 낮은 `num-md`(16px)를 대표 타이포로 쓴다. 그리드 스팬·최소 높이·패딩만으로는 두 등급이 나란히 배치됐을 때 어느 쪽이 더 비중 있는 정보인지 구분되지 않으므로, 숫자 크기 자체가 등급 신호의 1차 채널이 되도록 강제한다. Hero(`num-display` 32px) → Standard(`num-lg` 24px) → Compact/Row(`num-md` 16px)로 이어지는 3단 위계를 어떤 조합에서도 깨지 않는다.

### 8-3. Compact 카드 와이어프레임 (예시 데이터)

```
┌────────────────────────────┐
│ S&P 500        07/09 마감 기준│  ← 라벨 12px + 신선도 캡션
│ 6,214.85                    │  ← num-md 16px, text.primary
│ ▲ +32.14 (+0.52%)  ∿∿∿∿╱   │  ← 등락 13px market.up + 스파크라인 96×28px
└────────────────────────────┘
```

### 8-4. Row 예시 (관심종목)

```
│ 삼성전자  005930   ₩84,300   ▲ +0.95%   [스파크라인]  │ 52px
│ NVIDIA   NVDA     $192.42   ▼ −1.12%   [스파크라인]  │
```
- 열 정렬: 종목명 좌측, 숫자 열은 전부 우측 정렬(tabular). 행 호버 시 `surface.card-hover`, 클릭 시 Stock 상세로 이동.

### 8-5. 전역 공유 컴포넌트 계약 — 계좌/자산 배분 스택 바

"계좌별 자산 배분"(자산별 비중) 스택형 바는 Asset·Portfolio 등 복수 페이지가 동일한 정보(비중 breakdown)를 표현하는 **단일 전역 컴포넌트**다. 페이지별 색 예산 소진 여부 같은 우연한 사정으로 한쪽은 무채색, 다른 쪽은 유채색으로 그리는 것을 금지하고, 다음 계약을 모든 페이지가 예외 없이 상속한다.

| 항목 | 규칙 |
|---|---|
| 색 | 세그먼트 색은 `chart.series-1~5`(§2-7) 순서로 고정 배정한다. 항목이 6개 이상이면 6번째부터 `chart.series-5`(회색)를 재사용하고 라벨로 구분한다 |
| 라벨 | 세그먼트 폭이 충분하면 바 내부에, 좁으면 바 하단 범례에 계좌명·비중(%)을 **항상** 텍스트로 병기한다 — 색만으로 구분하지 않는다(§11-2와 동일 원칙) |
| 정렬 순서 | 비중 큰 순 → 작은 순으로 좌→우 배치, 동률 시 가나다순 |
| 높이 | 24px(단독 위젯) / 16px(카드 내부 축소형) |
| 호버 | 세그먼트 호버 시 Level 2 툴팁으로 정확한 금액·비중 표시 |
| 예외 금지 | 페이지별로 무채색/유채색을 임의로 다르게 적용하지 않는다. 색 예산(§2-6 R1) 소진 등 개별 페이지 사정으로 동일 컴포넌트의 색 처리를 바꾸지 않는다 |

이 컴포넌트는 Asset·Portfolio 문서가 공통으로 인용하는 전역 정의이며, 두 문서 모두 본 계약을 그대로 상속한다(개별 변형 금지). §2-7의 팔레트 재정의로 등락·상태 색과의 헥스 충돌이 해소되었으므로, 유채색 팔레트를 계좌/자산 배분처럼 순수 비교·구분 목적에 안전하게 재사용할 수 있다(P2의 ④ 차트 시리즈 구분 용도에 해당).

---

## 9. 상태 시스템 (States & Data Freshness)

### 9-1. 로딩 스켈레톤

- 스켈레톤은 최종 레이아웃과 동일한 골격(제목 바, 숫자 블록, 스파크라인 자리)을 유지한다 — 로드 후 요소 점프 금지.
- 블록: `skeleton.base` 배경, radius 4px(텍스트 라인 높이 12px) / 6px(차트 영역). 시머는 `skeleton.highlight`가 좌→우 1400ms 주기.
- 1.5초 초과 시 카드 헤더에 "불러오는 중" 캡션 추가. 스피너는 버튼 내부 로딩에만 사용.
- **잠금 콘텐츠 게이트(Asset·Portfolio 본문)**: 일반 로딩 스켈레톤과 달리, 배경에 실제 레이아웃 스켈레톤을 깔고 그 위에 `overlay.gate`(#000000 40%, §2-8)를 씌운다 — 모달 전용 `overlay.scrim`(60%)을 재사용하지 않는다. 60% 불투명 검정 위에서는 스켈레톤 블록의 형태가 거의 지각되지 않아 "점프 없는 해제"라는 목적을 달성하지 못하기 때문이다. 배경 스켈레톤 없이 순수 `overlay.gate`와 잠금 아이콘·CTA만 두는 구성도 허용되며, 이 경우 해제 시 콘텐츠는 페이드-인 + 8px 상승(§10-2 오버레이 등장 규칙)만으로 점프 없는 전환을 구현한다.

### 9-2. 데이터 신선도 (★ 핵심 규칙)

> **N1. 타임스탬프 없는 시세·지표 숫자는 렌더링 금지.** 모든 데이터 카드는 as-of 시각(`07/10 07:12 KST` 형식)을 헤더 또는 값 옆에 반드시 표기한다.
> **N2. 신선도는 4단계로 자동 판정하고 시각적으로 구분한다.**

| 상태 | 판정 기준(데이터별 기대 갱신 주기 T 대비) | 시각 표현 |
|---|---|---|
| **FRESH** | 경과 < T | 값 `text.primary`, 배지 없음 (세션 중이면 라이브 점 허용) |
| **DELAYED** | T ≤ 경과 < 3T | "지연" 배지(`accent.yellow` 아웃라인, 12px), 값은 흰색 유지 |
| **STALE** | 경과 ≥ 3T | **값 전체를 `text.secondary`로 강등** + "오래된 데이터" 배지(`accent.yellow` 채움 12%), 툴팁에 마지막 갱신 시각 |
| **CLOSED-SNAPSHOT** | 해당 시장·세션 마감 후 | 값 `text.primary` 유지하되 "07/09 마감 기준" 라벨 필수, 라이브 점·펄스 전면 금지 |

### 9-3. 빈 상태 · 에러 상태

- **빈 상태**: 카드 중앙에 아이콘 32px `text.tertiary` + 메시지 1줄(`type.body`, `text.secondary`) + 필요 시 액션 링크 1개. 예: "오늘 작성된 매매일지가 없습니다 — 새 일지 작성".
- **에러 상태**: `status.danger` 아이콘 20px + 원인 메시지 + "다시 시도" 고스트 버튼. **캐시된 직전 데이터가 있으면 빈 화면 대신 STALE 스타일로 이전 값을 유지 표시**하고 에러는 배지로 알린다(정보 제로 상태 회피 원칙).

### 9-4. 세션 인식 렌더링 (★ 야간선물 사고 방지 규칙)

배경: 야간선물(CME 연계 등) 값이 시장 마감 후에도 흰색 큰 숫자로 남아 "현재 실시간 값"처럼 오독된 실제 사고가 있었다. 이를 컴포넌트 계약으로 구조적으로 차단한다.

> **S1. 모든 시세 데이터 소스는 '유효 세션' 정의를 갖는다.** (예: 야간선물 18:00–다음날 05:00 KST, 미국 정규장 09:30–16:00 ET)
> **S2. 현재 시각이 유효 세션 밖이면 '라이브처럼 보이는' 스타일을 금지한다** — 라이브 점, 펄스, 흰색 대형 숫자 단독 표시 모두 불가. 값은 CLOSED-SNAPSHOT 형식("07/10 05:00 마감 기준")으로만 표시한다.
> **S3. 등락률은 반드시 기준가 기준을 병기한다.** 예: "▲ +0.42% (전일 정규장 종가 대비)". 기준가를 알 수 없으면 등락률 자체를 숨기고 절대값만 표시한다.
> **S4. 세션 경계(개장 직전·마감 직후)에는 상태 전환을 값 갱신보다 우선한다** — 새 데이터가 오기 전이라도 마감 시각이 지나면 즉시 CLOSED-SNAPSHOT 스타일로 전환한다.

---

## 10. 인터랙션 · 모션

### 10-1. 지속시간·이징 표준

| 토큰 | 값 | 대상 |
|---|---|---|
| `motion.fast` | 120ms | 호버, 버튼 상태 변화 |
| `motion.base` | 200ms | 패널·드롭다운 등장, 탭 전환 |
| `motion.slow` | 250ms | 모달, 사이드바 접힘/펼침 |
| 이징 | 감속 곡선(빠르게 시작, 부드럽게 정지) 단일 표준 | 전 컴포넌트 공통 |

### 10-2. 규칙

모션 예산은 명시적으로 둔다. 이 목록에 없는 움직임은 추가하지 않는다.

| 대상 | 지속 | 횟수 |
|---|---|---|
| 카드 등장(아래에서 8px + 페이드) | 240ms | 첫 렌더 1회, 카드당 40ms 스태거(최대 10칸) |
| 핵심 수치 카운트업 | 520ms | 페이지당 **1개**(화면 최상위 수치)만 |
| 차트 선 그리기 | 300ms | 첫 렌더 1회(리사이즈 재렌더에서는 생략) |
| 값 변경 플래시 | 1.2s | 값이 **실제로 바뀌었을 때만** 1회 |
| 서랍 열기/닫기 | 250ms | — |
| 라이브 점·차트 끝점 펄스 | 2s 주기 | 데이터가 FRESH일 때만 무한 |

- 스크롤 연동 애니메이션·패럴랙스·바운스는 **전면 금지**를 유지한다. 등장은 첫 진입 1회로 끝낸다 — 매일 보는 도구에서 스크롤할 때마다 요소가 나타나면 값을 읽으려 할 때마다 기다리게 된다.
- 무한 반복은 라이브 펄스와 스켈레톤 시머만 허용한다.
- 값 변경 플래시는 배경 워시만 쓰고 글자색은 건드리지 않는다(등락색은 등락률이 이미 쓰고 있다). **연속 점멸 금지.**
- 카운트업은 마스킹된 금액에는 걸지 않는다 — 세는 과정에서 값이 드러나면 마스킹이 무의미하다.
- OS의 "동작 줄이기" 설정 시 모든 모션 제거(즉시 전환), 시머는 정적 톤 변화로 대체.

> **M1. 등장 연출의 기본값은 "보이는 상태"여야 한다.** 요소를 투명하게 만들어 두고 나중에 보이게 하는 방식(JS 클래스 토글, `animation-fill-mode: both`)은 애니메이션이 시작되지 않는 상황 — 백그라운드 탭, rAF 정지, 스크립트 실패 — 에서 **콘텐츠가 영영 사라진다**. 실제로 두 번 발생했다(실측). 애니메이션은 돌 수 있다고 확인된 경우에만 켜지는 **옵트인**으로 구현하고, 꺼진 경로에서 화면이 정상이어야 한다.

### 10-3. 호버·포커스

- 호버 기본은 배경 변화(`surface.card-hover`) + 보더 밝기(`border.strong`)다.
- **클릭 가능한 카드에 한해** 2px 상승 + 그림자를 허용한다. 클릭할 수 없는 카드에는 금지 — 움직이는데 눌리지 않으면 그게 더 나쁜 신호다.
- 버튼·칩은 눌림에 1px 하강으로 응답한다(`:active`).
- 포커스: 모든 인터랙티브 요소에 `focus.ring`(2px #ECCB8A, offset 2px) 일관 적용. 마우스 클릭 포커스에는 링 생략, 키보드 포커스에만 표시.

---

## 11. 접근성 (Accessibility)

### 11-1. 대비율 기준

- 본문 텍스트: WCAG AA **4.5:1 이상**, 대형 텍스트(18px+ 또는 14px Bold+)·UI 요소: **3:1 이상**.
- 실측 조합(`surface.card` #1E2026 기준): `text.primary`(#F8F9FA) **15.45:1**, `text.secondary`(#A0A5B1) **6.60:1**, `text.tertiary`(#858C9B) **4.82:1**, `brand.primary`(#DFB76C) **8.63:1**.
- 구 팔레트 대비 `text.tertiary`가 3.2:1 → **4.82:1**로 올라가 본문 기준을 통과한다.
- 등락 색: `market.up` 한국 모드(#EF4444) **4.33:1**로 본문 기준에 근소 미달, 글로벌 상승(#10B981) 6.42:1, 한국 하락(#6BB6FF) 5.07:1. 미달분은 R4가 강제하는 **▲▼ 기호 병행** + 13px 이상 + 500 굵기로 보완한다(색 단독으로 방향을 전달하지 않는다).
- 골드 CTA 위 텍스트는 `text.inverse`(#141519)로 **9.67:1** — 흰 글자는 골드 위에서 읽히지 않으므로 금지한다.

### 11-2. 색맹 대응

- 등락은 색 단독 전달 금지(R4). ▲▼ 기호와 +/− 부호가 1차 정보, 색은 보조 채널.
- 한국 모드의 빨강/파랑 쌍은 적록색맹에게 글로벌 모드(초록/빨강)보다 유리하다 — 기본값 결정(§2-5)의 부수 이점.
- 차트에서 시리즈 구분은 색 + 라벨 직접 표기(범례 의존 최소화), 필요 시 선 스타일(실선/점선) 병행.
- 상태 배지는 색 + 텍스트 라벨("지연", "마감 기준") 병행 — 아이콘·색 단독 배지 금지.

### 11-3. 키보드 내비게이션

| 키 | 동작 |
|---|---|
| `Ctrl/⌘ + K` | 글로벌 검색(커맨드 팔레트) |
| `Tab / Shift+Tab` | 논리적 순서(사이드바 → 헤더 → 콘텐츠 좌상→우하) 포커스 이동 |
| `Esc` | 오버레이 닫기(모달 → 팝오버 → 검색 순 역순 해제) |
| `[` | 사이드바 접기/펼치기 |
| `↑↓ + Enter` | 리스트·검색 결과 탐색 및 진입 |

- 첫 Tab에서 "본문으로 건너뛰기" 스킵 링크 제공.
- 모달 열림 시 포커스 트랩, 닫힘 시 트리거 요소로 포커스 복귀.
- 최소 히트 영역 32×32px. 스크린리더용으로 등락 값은 "플러스/마이너스"를 읽을 수 있는 레이블 텍스트를 제공한다(기호만 노출 금지).

---

## 12. 개선 포인트 (구현 제약 관련 — 요약)

- 서비스는 정적 GitHub Pages 기반(서버·DB 없음)이므로, §9의 신선도 판정은 **빌드 시점에 기록된 as-of 타임스탬프 + 열람 시점의 클라이언트 시계 비교**로 수행하는 것을 전제로 설계했다. 데이터 파일에는 값과 함께 생성 시각·유효 세션 메타가 항상 포함되어야 한다.
- Asset·Portfolio의 비밀번호 게이트는 정적 환경 특성상 열람 차단(프라이버시 가림) 수준임을 Settings 내 고지 문구로 명시한다.
- 갱신 주기가 배치(모닝리포트 1일 1회, 뉴스 수 시간 간격)인 데이터는 FRESH 기준 주기 T를 소스별로 다르게 설정한다 — 실시간 소스와 동일 잣대 금지.

---

**문서 끝. 이후 01~10 페이지 설계 문서는 본 문서의 토큰명·수치를 변경 없이 인용한다. 변경이 필요하면 본 문서를 먼저 개정한다.**
