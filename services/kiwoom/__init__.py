"""Kiwoom OpenAPI+ 서비스 계층.

⚠️ 키움 OpenAPI+ OCX는 **Windows 32-bit Python + KOA 설치** 환경에서만 동작한다.
정적 사이트 생성(GitHub Actions)과 분리되어 있으며, 데스크톱(PySide6)에서 실행해
체결을 data/trades/trades.json에 적재한다. 그 파일이 커밋되면 Actions가 대시보드를 재생성한다.
"""
