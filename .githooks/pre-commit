#!/bin/sh
# 자산 평문 유출 가드(design/20 Phase 8) — 설치: git config core.hooksPath .githooks
# 로컬 전용 1차 방어선. github-actions bot 커밋은 이 훅을 거치지 않으므로 CI 게이트가
# 최종 방어선이다(scripts/check_no_plaintext_assets.py를 두 곳이 동일하게 재사용).
set -e
python scripts/check_no_plaintext_assets.py
